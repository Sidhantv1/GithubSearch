package com.example.githubsearch

/**
 * Constants used in the code
 */
object Constants {
    // Page parameter sent in request to the api
    const val PAGE = 1
    // Limit parameter to display no of result per api response
    const val LIMIT = 10
    // DB limit setup to limit the no enteries saved in the data base.
    const val DB_LIMIT = 20
}